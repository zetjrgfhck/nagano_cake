<div class="container">
  <div class="row>">
    <div class="col-lg-6 mx-auto center-block">
      <h5>
        <p>ようこそ、ながのCAKEへ！</p>
        <p>このサイトは、ケーキ販売のECサイトになります。</p>
        <p>会員でない方も商品の閲覧はできますが、</p>
        <p>購入には会員登録が必要になります。</p>
      </h5>
    </div>
  </div>

<div class="row">
  <div class="col-lg-6 mx-auto center-block">
 <table>
          <tr>
            <th>商品イメージ</th>  <th>商品名</th>  <th>税抜き価格</th>
          </tr>
            <% @items.each do |item| %>
          <tr>
            <td>
             <%= image_tag item.image, size: "100x100" %>
            </td>
            <td>
              <%= link_to item_path(item.id) do %>
              <%= item.name %>
              <% end %>
            </td>
            <td>
              <%= item.price %>
            </td>
          </tr>
           <% end %>
      </table>
    </div>
    </div>
    <%= paginate @items %>
</div>